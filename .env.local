# Local backend (Django dev server started via .claude/launch.json)
NEXT_PUBLIC_API_BASE = http://localhost:8001/api

# Deployed backend (Railway) — restore this line to use production instead:
# NEXT_PUBLIC_API_BASE = https://aiprojectevaluatorbackend-production.up.railway.app/api
